---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div
    class=`flex flex-col items-center w-full h-full rounded-xl p-4 backdrop-blur-md bg-background/10 dark:bg-foreground/5 shadow ring-2 ring-foreground/5 dark:ring-foreground/10 ${className}`
>
    <slot />
</div>
